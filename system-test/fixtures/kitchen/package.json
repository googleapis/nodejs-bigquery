{
  "name": "google-cloud-bigquery-fixture",
  "private": true,
  "scripts": {
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc -p .",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check",
    "start": "node build/src/index.js"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "@google-cloud/bigquery": "file:./google-cloud-bigquery.tgz"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "typescript": "^4.6.4",
    "gts": "^5.0.0"
  }
}
